# Downloaded code. Big, and rebuilt by `npm install`, so it never goes to GitHub.
node_modules/

# Next.js build output.
.next/
out/

# Your S3 address. Stays on your computer on purpose — see START-HERE.md step 6.
.env.local

# The design project this site was built from. Reference only, not part of the site.
Paladin_Home_Master_Source/

# Local stand-in portraits, so the site can be looked at before S3 exists.
# Content belongs on S3, never in the repo. See lib/assets.ts.
public/dev-assets/

# Operating system clutter.
.DS_Store
Thumbs.db

next-env.d.ts
*.tsbuildinfo

.vercel
